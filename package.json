{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "(if exist dist rmdir /s /q dist) & cd ../notes-frontend & npm run build & robocopy ../notes-frontend/dist ../notes-backend/dist /e",
    "deploy:full": "npm run build:ui & git add . & git commit -m uibuild & git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
